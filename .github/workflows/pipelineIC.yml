name: workflow
on:
   push:
     branches:
       - feat-movie
jobs:
  SAST:
    runs-on: ubuntu-latest
    steps: 
    - name: Primer paso
        run: echo "Hola, este es el primer paso del workflow"
name: Pipeline Laboratorio Final

on:
  push:
    branches:
       -feat-movie

jobs:
  Despliegue:
    runs-on: ubuntu-latest

    steps:
      - name: instalando Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Clonando repositorio
        uses: actions/checkout@v2

      - name : Build
        run: |
         chmod 777 gradlew
        # pip install -r requirements.txt